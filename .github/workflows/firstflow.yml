name: First workflow
on: worflow_dispatch
jobs:
   first-job:
       runs-on: ubuntu-latest
       steps:
        - name: print greeting
          run: echo "HELLO WORLD"
        - name: bye
          run: echo "Done"
       
