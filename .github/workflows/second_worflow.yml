name: second workflow
on: push
jobs:
 first_job:
   runs-on: ubuntu-latest
   steps:
    - name: get-code
      uses: actions/checkout@v3
    - name: download
      uses: actions/setup-node@v3
      with: 
           node-version: 18
